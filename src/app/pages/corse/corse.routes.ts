import { Routes } from '@angular/router';
import { CoursesComponent } from './cors.component';

export const corseroutes: Routes = [
    {
        path: '',
        component:CoursesComponent
        
    }
]